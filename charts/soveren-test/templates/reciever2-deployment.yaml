apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-receiver2
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}
    spec:
      containers:
        - name: receiver2
          image: {{ .Values.receiver2.image.name }}:{{ .Values.receiver.image.tag }}
          ports:
            - containerPort: 8080